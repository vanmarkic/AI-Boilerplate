/**
 * Domain config presets have moved to the database (tfc_domain_configs table).
 * Use the /api/domain-configs API to list and fetch domain configurations.
 *
 * The canonical type definitions remain in '../types/domain'.
 */
