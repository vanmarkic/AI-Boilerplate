import { Routes } from '@angular/router';
import { EventsTimelineComponent } from './events-timeline.component';

export const EVENTS_TIMELINE_ROUTES: Routes = [
  { path: '', component: EventsTimelineComponent },
];
