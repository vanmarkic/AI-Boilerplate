name: Frontend CI

on:
  push:
    branches: [master]
    paths: ['apps/main/frontend/**', 'packages/**', 'shared/openapi.json']
  pull_request:
    branches: [master]
    paths: ['apps/main/frontend/**', 'packages/**', 'shared/openapi.json']

permissions:
  contents: read

env:
  NODE_VERSION: '22'

defaults:
  run:
    working-directory: apps/main/frontend

jobs:
  install:
    name: Install & Generate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm i
        working-directory: .
      - run: bash shared/scripts/generate-frontend.sh
        working-directory: .
      - uses: actions/cache/save@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}

  type-check:
    name: Type Check
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache/restore@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}
      - run: npm i
        working-directory: .
      - run: npm run lint:tsc:all

  lint:
    name: Lint
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache/restore@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}
      - run: npm i
        working-directory: .
      - run: npm run lint:all

  test:
    name: Unit Tests
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache/restore@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}
      - run: npm i
        working-directory: .
      - run: npm run test:ci

  build:
    name: Build
    needs: [type-check, lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache/restore@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}
      - run: npm i
        working-directory: .
      - run: npm run build

  e2e:
    name: E2E Tests
    needs: [type-check, lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache/restore@v5
        with:
          path: |
            node_modules
            apps/main/frontend/node_modules
            packages/design-system/node_modules
            apps/main/frontend/src/app/shared/api/generated
          key: frontend-${{ github.sha }}
      - run: npm i
        working-directory: .
      - run: npx playwright install --with-deps chromium
      - run: npm run e2e
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: apps/main/frontend/playwright-report/
          retention-days: 7
