import { Routes } from '@angular/router';
import { AdminPermissionsComponent } from './admin-permissions.component';

export const ADMIN_PERMISSIONS_ROUTES: Routes = [
  { path: '', component: AdminPermissionsComponent },
];
