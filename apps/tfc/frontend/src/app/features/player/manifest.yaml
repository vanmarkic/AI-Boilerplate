name: player
tier: 1
description: Player view — events, decisions, recommendations, and score display
version: 0.2.0
dependencies:
  internal: [shared]
  external: []
api_endpoints: []
models: []
events_emitted: []
events_consumed: []
business_rules:
  - "Player sees active events and their descriptions"
  - "Player responds to decisions via the decision panel"
  - "In collaborative mode, advisors submit recommendations; decision-maker submits rulings"
  - "Player role (advisor/decision-maker) is resolved from scenario context on init"
  - "participant_id and role are passed from waiting room via route query params"
