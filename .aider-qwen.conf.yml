# Aider — Qwen 2.5 Coder profile
# Usage: aider --config .aider-qwen.conf.yml [files...]

model: ollama/qwen2.5-coder
# Ollama defaults to http://localhost:11434
# Override: set OLLAMA_API_BASE env var

# Commits
auto-commits: false
dirty-commits: false
attribute-author: true
attribute-committer: false

# Output
pretty: true
stream: true
show-diffs: true

# Behaviour
suggest-shell-commands: false
watch-files: false

# Read-only context (always loaded, never edited)
read:
  - AGENTS.md
  - backend/AGENTS.md
  - frontend/AGENTS.md
  - shared/openapi.yaml
