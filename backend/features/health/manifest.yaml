name: health
tier: 1
description: Health check endpoint for readiness/liveness probes
version: 0.1.0
dependencies:
  internal: []
  external: []
api_endpoints:
  - GET /api/health
models: []
events_emitted: []
events_consumed: []
business_rules:
  - "Always returns status 'ok' and the application version"
  - "No authentication required"
  - "Must respond in < 100ms"
